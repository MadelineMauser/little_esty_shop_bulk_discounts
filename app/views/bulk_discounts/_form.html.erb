<%= form_with model: model, method: method, url: url, class: 'merchant_bulk_discount_form', local: true do |f| %>
  <%= f.label :percentage_discount, 'Percentage Discount' %>
  <%= f.number_field :percentage_discount %><br>
  <%= f.label :quantity_threshold, 'Quantity Threshold' %>
  <%= f.number_field :quantity_threshold %><br>
  <%= f.submit 'Submit'%>
<% end %>
